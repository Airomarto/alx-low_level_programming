#include <stdio.h>

/**
 * main - this is the Entry point
 *
 * Return (0) is constant to depict it has been executed
 */
int main(void)
{
		puts("\"Programming is like building a multilingual puzzle");
		return (0);
}
