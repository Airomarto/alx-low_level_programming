#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char b = 97;

	while (b <= 122)
	{
		if (b != 113 && b != 101)
			putchar(b);
		x++;
	}
	putchar(10);
	return (0);
}
